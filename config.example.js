// config.example.js
// Rename this file to config.js and replace with your actual values
const CONFIG = {
    paypal: {
        clientId: {
            sandbox: 'YOUR_SANDBOX_CLIENT_ID',
            production: 'YOUR_PRODUCTION_CLIENT_ID'
        },
        mode: 'sandbox' // Change to 'production' for live payments
    }
};
